<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG BUILDS ONLY: allow plain HTTP to this device's loopback, so the Claude features can be tested
     against tools/mock-claude-api.mjs (via `adb reverse tcp:8787 tcp:8787`). Release builds keep
     Android's default: HTTPS only. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
